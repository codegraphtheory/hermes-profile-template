name: Validate Hermes Profile Template

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    uses: ./.github/workflows/validate-profile.yml
    with:
      python-version: "3.11"
      run-smoke-test: true
