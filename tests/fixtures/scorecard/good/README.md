# Example Profile

Install this profile:

```bash
hermes profile install github.com/your-org/example-profile --alias example
```

Validate before publishing:

```bash
python3 scripts/validate_profile.py .
make validate
```
